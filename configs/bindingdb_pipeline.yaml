name: C2DTI_BINDINGDB_PIPELINE
protocol: P1
output:
  base_dir: outputs
dataset:
  name: BindingDB
  path: datasets/BindingDB_Kd.csv
  allow_placeholder: false
model:
  name: simple_baseline
perturbation:
  strength: 0.2
  seed: 42
causal:
  enabled: true
  weight: 1.0