name: bindingdb_binary_baseline
protocol: P_binary

dataset:
  name: BindingDB
  path: datasets/BindingDB_Kd_binary.csv
  allow_placeholder: false

model:
  name: simple_baseline

split:
  strategy: random
  test_ratio: 0.2
  seed: 42

binary:
  threshold: 0.5

output:
  base_dir: outputs_binary
